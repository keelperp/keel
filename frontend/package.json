{
  "name": "keel-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "abi": "node scripts/gen-abi.mjs",
    "dev": "npm run abi && vite",
    "build": "npm run abi && tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}